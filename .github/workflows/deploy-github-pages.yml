# Static export was removed (Vercel dynamic + Postgres). Deploy this app via Vercel.
# Enable manually if you restore `output: 'export'` and a GitHub Pages–compatible build.
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "GitHub Pages deploy is disabled. Use Vercel for this project."
          exit 0
